# セキュリティ方針

本リポジトリ（people_relation）のセキュリティに関する連絡先と、報告・開示の取り扱いを定めます。

## 公開リポジトリであることの前提

本リポジトリは **GitHub 上の公開（public）リポジトリ** です。次の点を前提に方針を置いています。

- **ソースと履歴は誰でも閲覧できる**ため、脆弱性の詳細を Issue・Discussion・Pull Request・コミットメッセージなど **公開の場に書くと、即座に世界中に伝播**します。必ず **非公開の経路**（下記のプライベート報告など）でご連絡ください。
- **本リポジトリの修正は上流（このリポジトリの `main`）に対して行います。** フォークや独自デプロイの運用者は、各自で更新の取り込み・運用責任を負います（公開リポジトリの一般的な取り扱いです）。
- **認証情報や本番用シークレットをリポジトリに含めないでください。** 公開リポジトリでは誤コミットの影響が大きくなります。万一コミットしてしまった場合は、ローテーションと履歴からの除去（必要に応じて GitHub サポートの利用）を検討してください。

## 脆弱性の報告

**公開の Issue や Pull Request では脆弱性の内容を書かないでください。**（悪用される可能性があります）

次のいずれかで、**非公開**にご連絡ください。

1. **GitHub のプライベート脆弱性報告（推奨）**  
   リポジトリの **Security** タブから **Report a vulnerability** を選び、内容を送信してください。  
   [Create a private security advisory](https://github.com/5avvadaJ1r0/people_relation/security/advisories/new)

2. 上記が利用できない場合  
   メンテナが別途案内する連絡手段（メール等）に、同様に非公開で送ってください。

### 報告に含めてほしい情報（任意だが助かります）

- 影響を受けるコンポーネント（例: バックエンド API、フロント、Docker 構成など）
- 再現手順、または再現に必要な最小の情報
- 想定される影響（情報漏えい、権限昇格、DoS など）
- 既知の修正案やパッチ案（あれば）

## 対応方針（開示）

- 報告を受け取ったら、内容の確認と優先度付けを行い、可能な範囲で修正スケジュールを共有します。
- **技術的な詳細や CVE・GitHub Security Advisory の公開は、修正が利用者に届けられる状態になったあと**（原則として修正が `main` にマージされた後など）に、報告者と調整のうえ行います。
- 報告者のクレジットを希望する場合、リリースノート等で記載できるよう調整します。

## サポート対象のバージョン

現時点では **Git のデフォルトブランチ（`main`）上の最新の状態** をセキュリティ修正の対象とします。古いコミットやフォーク独自の変更については、個別に判断します。

| 対象 | セキュリティ修正の提供 |
|------|------------------------|
| `main` ブランチの最新 | はい |
| それ以外のブランチ・タグ | 状況に応じて判断 |

## スコープの目安

次のような事項は、本ポリシーで扱う「脆弱性」の報告として歓迎します。

- 認証・認可の不備、機密情報の露出
- インジェクション、パストラバーサル等のアプリケーション脆弱性
- 依存パッケージの既知の深刻な脆弱性に関する指摘（Dependabot 等と併せて対応）

次のものは **別窓口** になることがあります（それでも連絡いただいて構いません）。

- Wikipedia / Wikidata 本体のポリシーやレート制限（各プロジェクトの窓口に従ってください）
- デプロイ先インフラ固有の設定ミス（利用者側の環境に依存する場合）

## メンテナ向け（GitHub リポジトリ設定）

公開リポジトリでは、次を有効にしておくと報告・対応がしやすくなります。

1. **Settings** → **Code security and analysis**
   - **Private vulnerability reporting** — 外部からの非公開報告の受け口（**公開リポジトリでは特に推奨**）。
   - **Dependabot alerts** — 依存関係の既知脆弱性の検知（公開リポジトリでも利用可能）。
   - 必要に応じて **Dependabot security updates** — アラートに基づく修正用 PR の自動作成。

依存関係のバージョン更新 PR は `.github/dependabot.yml`（Dependabot version updates）で受け取れるようにしています。セキュリティ関連のアラート対応とは併用してください。
